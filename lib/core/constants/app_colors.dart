import 'package:flutter/material.dart';

// ── Primary palette — Playful Minimalist ─────────────────────────────────────
const Color appCoral      = Color(0xFFFF8C69);
const Color appPeach      = Color(0xFFFFDDD0);
const Color appCream      = Color(0xFFFFF8F5);
const Color appInkDark    = Color(0xFF2C2C2C);
const Color appInkMid     = Color(0xFFADB5BD);
const Color appDivider    = Color(0xFFF0EDE8);

// ── Weather ───────────────────────────────────────────────────────────────────
const Color appSunYellow  = Color(0xFFF7C948);
const Color appTempGrey   = Color(0xFF8A9377);
const Color appWarmOrange = Color(0xFFE0976C);

// ── Search bar ────────────────────────────────────────────────────────────────
const Color appSearchGrey   = Color(0xFF9AA088);
const Color appFmGreen      = Color(0xFF2E8B57);
const Color appFmDark       = Color(0xFF2B2E26);
const Color appClearBg      = Color(0xFFF0F1E6);
const Color appClearIcon    = Color(0xFF7A8169);
const Color appSectionHead  = Color(0xFFA7AD95);
const Color appSuggHover    = Color(0xFFF5F6EC);

// ── Map ───────────────────────────────────────────────────────────────────────
const Color appMapBackground = Color(0xFFE9EED9); // shown while tiles load

// ── Crowd indicators ─────────────────────────────────────────────────────────
const Color appCrowdGreen  = Color(0xFF2FA76B);
const Color appCrowdOrange = Color(0xFFE59B36);
const Color appCrowdRed    = Color(0xFFE0786C);

// ── User marker ───────────────────────────────────────────────────────────────
const Color appMarkerRing = Color(0xFF9C97E6);
const Color appMarkerInk  = Color(0xFF3B3960);

// ── Auth / profile ────────────────────────────────────────────────────────────
const Color appLavender = Color(0xFFC9C6EE); // logged-in border & avatar bg
const Color appMossText = Color(0xFF5C6150); // secondary button text
const Color appMascotBg = Color(0xFFE9EED9); // mascot circle bg
